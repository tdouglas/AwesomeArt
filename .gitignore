# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile ~/.gitignore_global

# Ignore bundler config
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3

# Ignore all logfiles and tempfiles.
/log/*.log
/tmp

  <%= render '..application/stop' %>
 << artwork %>
  <% end %>

    <% array = [] %>
  <% artwork = {} %>

  <% @images_array.each do |info| %>
    <% t = info[:title] %></br>
    <% a = info[:artist] %></br>
    <% m = info[:medium] %></br>
    <% c = info[:collection] %></br>
    <% artwork[:title] = t %>
    <% artwork[:artist] = a %>
    <% artwork[:medium] = m %>
    <% artwork[:collection] = c %>
    <% array << artwork %>
    <%= array.class %>
  <% end %>

  <% @images_array.each do |info| %>
    Title: <%= info[:title] %></br>
    Artist: <%= info[:artist] %></br>
    Medium: <%= info[:medium] %></br>
    Collection: <%= info[:collection] %></br>
  <% end %>


 Title: <p><%= @images_array[0][:title] %></p>
 Artist: <%= @images_array[0][:artist] %>
 Medium: <%= @images_array[0][:medium] %>
 Collection: <%= @images_array[0][:collection] %>